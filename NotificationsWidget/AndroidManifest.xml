<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roymam.android.notificationswidget">

    <uses-sdk android:minSdkVersion="14"
              android:targetSdkVersion="17"/>
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application android:label="Notifications Widget" android:allowBackup="true">
        <activity android:label="@string/title"
              android:name="NotificationActivity"
              android:theme="@android:style/Theme.NoDisplay"
              android:excludeFromRecents="true">
    	</activity>
    	<activity android:label="@string/settingstitle"
              android:name="SettingsActivity"
              android:theme="@android:style/Theme.Holo">              
    	</activity>
    
        <!-- The widget provider -->
        <receiver android:name="NotificationsWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.roymam.android.notificationswidget.update_clock" />
                <action android:name="com.roymam.android.notificationswidget.activate_service" />
                </intent-filter>
            <!-- This specifies the widget provider info -->
            <meta-data android:name="android.appwidget.provider"
                    	android:resource="@xml/widgetinfo" />
        </receiver>
        <service android:name="NotificationsWidgetService"             
            	 android:permission="android.permission.BIND_REMOTEVIEWS"/>
                    
        <service android:name="NotificationsService"
            	 android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
      			 android:label="@string/notifications_service"
      			 >
	    	<intent-filter>
	      		<action android:name="android.accessibilityservice.AccessibilityService" />
	    	</intent-filter>
	    	<meta-data
    			android:name="android.accessibilityservice"
    			android:resource="@xml/notifications_service_config" />
	  	</service>
    </application>
</manifest>
